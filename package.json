{
  "name": "augmented-workflow",
  "version": "0.12.0",
  "private": true,
  "description": "Repo-native AI workflow operating model: portable AGENTS.md, skills, and durable context.",
  "scripts": {
    "prepare": "husky",
    "gate:check": "node .scripts/aw-gate.js check",
    "gate:test": "node test/gate/receipt.test.js && node test/gate/suggest-e2e.test.js && node test/gate/e2e-coverage.test.js && node test/gate/learning-audit.test.js",
    "trace:suggest-e2e": "node .scripts/aw-gate.js trace --suggest-e2e",
    "trace:check": "node .scripts/aw-gate.js trace",
    "workflow:check": "node .scripts/aw-gate.js workflow-check",
    "pin:check": "node .scripts/aw-gate.js pin check",
    "pin:run": "node .scripts/aw-gate.js pin run"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
