.DS_Store

# Transient lock created by the Stop hook while it writes a session log
.claude/hooks/.aw-log-session-active

# Per-checkout freshness markers written by .scripts/aw-gate.js record
.aw-gate-state.json

# Per-checkout proof-of-work receipts consumed by .scripts/aw-gate.js record
.aw/receipts/

# Local cache of the org-shared knowledge repo (aw-gate.js org-sync)
.aw-org-cache/

# Temporary trace annotation intent batches
.aw/tmp/

# Local workflow execution breadcrumbs
.aw/workflow-trace.jsonl

# Local behavior pin worktrees and run logs
.aw/pin/

# Per-checkout skill tracking session id (aw-gate.js track)
.aw/session

# npm dependencies (husky)
node_modules/

# Isolated feature worktrees created by aw-create-worktree
.worktrees/
